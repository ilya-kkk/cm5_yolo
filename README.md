# YOLOv8 Camera Stream with Hailo 8L

Этот проект реализует обработку видео с камеры CSI на Raspberry Pi CM5 с модулем Hailo 8L, используя модель YOLOv8 для детекции объектов и стриминг обработанного видео через libcamera-vid.

## 🚀 Возможности

- 📹 Захват видео с CSI камеры в разрешении 1920x1080 @ 30fps
- 🤖 Обработка видео с помощью YOLOv8 на ускорителе Hailo 8L
- 🎯 Детекция объектов с отображением bounding boxes, классов и вероятностей
- 📊 Отображение FPS в реальном времени
- 🌐 Стриминг обработанного видео через UDP на указанный IP-адрес

## 📋 Требования

- Raspberry Pi CM5 с модулем Hailo 8L
- CSI камера
- Docker и Docker Compose
- Сеть с доступом к IP-адресу 192.168.0.173:5000
- Ubuntu 22.04 или совместимая ОС

## 🛠️ Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd cm5_yolo
```

2. **Убедитесь, что файл `yolov8n.hef` находится в корневой директории проекта.**

3. **Сделайте скрипты исполняемыми:**
```bash
chmod +x *.sh *.py
```

## 🚀 Запуск

### 🎯 Автоматический запуск (рекомендуется)
```bash
./start_yolo_stream.sh
```

Этот скрипт:
- Проверяет наличие всех необходимых файлов
- Тестирует Docker и Docker Compose
- Опционально тестирует камеру и Hailo
- Собирает Docker образ
- Запускает YOLO camera stream

### 🔧 Ручной запуск

#### 1. Тестирование компонентов
```bash
# Тест камеры
python3 test_camera.py

# Тест Hailo 8L
python3 test_hailo.py
```

#### 2. Сборка и запуск
```bash
# Сборка Docker образа
docker-compose build

# Запуск сервиса
docker-compose up
```

### 🛑 Остановка
Нажмите `Ctrl+C` для остановки стрима.

## ⚙️ Конфигурация

### Основные параметры
Основные параметры можно изменить в файле `main_camera_yolo.py`:

```python
stream = YOLOCameraStream(
    hef_path="yolov8n.hef",      # Путь к HEF файлу модели
    camera_index=0,               # Индекс камеры
    width=1920,                   # Ширина видео
    height=1080,                  # Высота видео
    fps=30                        # Частота кадров
)
```

### Сетевые настройки
IP-адрес и порт для стриминга можно изменить в методе `start_libcamera_stream()`:

```python
libcamera_cmd = [
    'libcamera-vid',
    '-t', '0',
    '--codec', 'h264',
    '--width', str(self.width),
    '--height', str(self.height),
    '--framerate', str(self.fps),
    '--inline',
    '-o', 'udp://192.168.0.173:5000'  # Измените здесь
]
```

## 📡 Стриминг

Обработанное видео стримится через UDP на адрес `192.168.0.173:5000` с использованием libcamera-vid.

### Просмотр стрима

#### VLC Media Player
```bash
vlc udp://@:5000
```

#### GStreamer
```bash
gst-launch-1.0 udpsrc port=5000 ! h264parse ! avdec_h264 ! autovideosink
```

#### FFmpeg
```bash
ffplay udp://:5000
```

## 📁 Структура проекта

```
.
├── main_camera_yolo.py              # Основной скрипт YOLO camera stream
├── simple_camera_yolo.py            # Упрощенная версия для тестирования
├── test_camera.py                   # Скрипт тестирования камеры
├── test_hailo.py                    # Скрипт тестирования Hailo 8L
├── yolov8n.hef                      # HEF модель YOLOv8
├── Dockerfile                       # Docker образ с Hailo
├── docker-compose.yml               # Docker Compose конфигурация
├── requirements.txt                  # Python зависимости
├── start_yolo_stream.sh             # Основной скрипт запуска
├── run_camera_yolo.sh               # Альтернативный скрипт запуска
├── quick_start.sh                   # Быстрый запуск
├── README.md                        # Документация
└── SETUP.md                         # Инструкции по настройке
```

## 🔍 Устранение неполадок

### ❌ Проблемы с камерой

**Симптомы:**
- Ошибка "Failed to open camera"
- Пустой экран или черные кадры

**Решения:**
1. Проверьте подключение CSI камеры
2. Убедитесь, что камера поддерживается libcamera
3. Проверьте права доступа к устройствам
4. Запустите `python3 test_camera.py` для диагностики

### ❌ Проблемы с Hailo

**Симптомы:**
- Ошибка "Failed to load model"
- Ошибка "Inference failed"

**Решения:**
1. Проверьте подключение модуля Hailo 8L
2. Убедитесь, что HEF файл совместим с вашей версией Hailo
3. Запустите `python3 test_hailo.py` для диагностики
4. Проверьте логи Docker контейнера

### ❌ Проблемы со стримингом

**Симптомы:**
- Видео не воспроизводится
- Ошибки сетевого подключения

**Решения:**
1. Убедитесь, что IP-адрес 192.168.0.173 доступен в сети
2. Проверьте, что порт 5000 не заблокирован файрволом
3. Убедитесь, что сеть поддерживает UDP трафик
4. Проверьте логи libcamera-vid

### ❌ Проблемы с Docker

**Симптомы:**
- Ошибка сборки образа
- Ошибка запуска контейнера

**Решения:**
1. Убедитесь, что Docker запущен
2. Проверьте доступ к интернету для загрузки базового образа
3. Убедитесь, что у вас есть права для запуска Docker
4. Проверьте свободное место на диске

## 📊 Логи и отладка

### Просмотр логов Docker
```bash
# Логи в реальном времени
docker-compose logs -f yolo-camera-stream

# Последние логи
docker-compose logs --tail=100 yolo-camera-stream
```

### Просмотр логов libcamera-vid
Логи libcamera-vid отображаются в консоли Docker контейнера.

### Отладка Python скриптов
Добавьте `print()` или используйте логирование для отладки:

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

## 🔧 Дополнительные настройки

### Изменение модели
Замените `yolov8n.hef` на другой HEF файл и обновите соответствующие параметры в коде.

### Изменение разрешения
Обновите параметры `width` и `height` в скрипте и убедитесь, что камера поддерживает выбранное разрешение.

### Изменение FPS
Обновите параметр `fps` в скрипте. Учтите, что более высокий FPS может снизить производительность.

## 📚 Дополнительные ресурсы

- [Hailo Platform Documentation](https://docs.hailo.ai/)
- [libcamera Documentation](https://libcamera.org/)
- [OpenCV Python Documentation](https://docs.opencv.org/)
- [Docker Documentation](https://docs.docker.com/)

## 🤝 Поддержка

При возникновении проблем:

1. **Проверьте логи** Docker контейнера
2. **Запустите тесты** компонентов
3. **Проверьте конфигурацию** сети и устройств
4. **Обратитесь к документации** Hailo
5. **Создайте issue** в репозитории проекта

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 🙏 Благодарности

- Hailo AI за предоставление ускорителя и SDK
- Raspberry Pi Foundation за платформу
- OpenCV и libcamera за библиотеки компьютерного зрения 